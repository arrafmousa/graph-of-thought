# Copy to `.env` for local execution. Never commit real secrets.
# Only variable NAMES are documented here; values must be provided locally.

# Optional external model/provider credentials (recorded only by name in the manifest).
# GOT_MODEL_API_KEY=
# GOT_MODEL_PROVIDER=
# GOT_MODEL_ENDPOINT=

# Azure OpenAI Batch judge credential.
AZURE_OPENAI_API_KEY=
